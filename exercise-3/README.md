# Ignore them

Learnt how to create a .gitignore file to ignore certain file types.

Used the following commands:
```
-code .gitignore <- Added *.o *.exe etc.
-git add .
-git commit -m "Added ignore instructions"
```
