# Merge conflict

Learnt how to manually fix merge conflicts that could arise due to branching.

Used the following commands:
```
-git merge another-piece-of-work
-# manually change the conflict that arose.
-git add equation.txt
-git commit -m "Merged branches and fixed conflict"
```